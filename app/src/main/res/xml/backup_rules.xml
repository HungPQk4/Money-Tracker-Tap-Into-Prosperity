<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude encrypted token storage from backups (keys are device-specific) -->
    <exclude domain="sharedpref" path="AuthPrefs"/>
</full-backup-content>
