# =================================================================
#  C?U H�NH TEST ? H2 In-Memory Database
#  File n�y CH? ???c d�ng khi ch?y: ./gradlew test
#  Ho�n to�n ??c l?p v?i Neon ? kh�ng ?nh h??ng d? li?u production
# =================================================================

# H2 in-memory, t? t?o khi test b?t ??u, t? x�a khi test k?t th�c
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;INIT=CREATE DOMAIN IF NOT EXISTS TIMESTAMPTZ AS TIMESTAMP WITH TIME ZONE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# =================================================================
#  JPA / HIBERNATE cho test
#  create-drop = t? t?o b?ng t? Entity, xong x�a lu�n
# =================================================================
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# =================================================================
#  JWT ? PH?I d�ng ?�ng t�n jwt.secret v� jwt.expiration
#  ?? kh?p v?i @Value("${jwt.secret}") trong JwtUtil.java c?a b?n
# =================================================================
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=3600000

# Khong chay schema.sql (Postgres-only: CREATE EXTENSION pgcrypto) khi test tren H2.
# Bang duoc Hibernate tao tu entity (ddl-auto=create-drop). KHONG anh huong production.
spring.sql.init.mode=never